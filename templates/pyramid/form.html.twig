{% extends 'base.html.twig' %}
{% block body %}
{{ form_start(form) }}
<div class="my-custom-class-for-errors">
    {{ form_errors(form) }}
</div>
        {{ form_row(form.Name) }}
        {{ form_row(form.Base) }}
    <button type="submit">create</button>
</div>
{{ form_end(form) }}
{% endblock %}